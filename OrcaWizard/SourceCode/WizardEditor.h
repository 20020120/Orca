#pragma once

// ------------------------------ エディターGui表示用のクラス ------------------------------
class WizardEditor
{
public:
    void Initialize();
    void Update(float Dt_);
    void Finalize();


private:
    
};